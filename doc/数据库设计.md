# 创建数据库
``` SQL
CREATE DATABASE smartone_quant 
CHARACTER SET utf8mb4 
COLLATE utf8mb4_unicode_ci;
``` 

# 创建板块列表存储表: id,sector_name,create_time,update_time , 建表语句包含中文注释
``` SQL
CREATE TABLE `qmt_sector` (
  `id` bigint(20) NOT NULL AUTO_INCREMENT COMMENT 'ID',
  `sector_name` varchar(255) COLLATE utf8mb4_unicode_ci NOT NULL COMMENT '板块名称',
  `create_time` timestamp NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  `update_time` timestamp NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
  PRIMARY KEY (`id`),
  KEY `qmt_sector_sector_name_index` (`sector_name`)
) ENGINE=InnoDB AUTO_INCREMENT=15735 DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='板块列表存储表'
```

# 创建板块成分股列表存储表：id,sector_id,stock_code,create_time,update_time , 建表语句包含中文注释
```SQL
CREATE TABLE `qmt_sector_stock` (
  `id` bigint(20) NOT NULL AUTO_INCREMENT COMMENT 'ID',
  `sector_id` int(11) NOT NULL COMMENT '板块ID',
  `stock_code` varchar(20) COLLATE utf8mb4_unicode_ci NOT NULL COMMENT '股票代码',
  `create_time` timestamp NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  `update_time` timestamp NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
  PRIMARY KEY (`id`),
  KEY `qmt_sector_stock_sector_id_index` (`sector_id`),
  KEY `qmt_sector_stock_stock_code_index` (`stock_code`)
) ENGINE=InnoDB AUTO_INCREMENT=680833 DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='板块成分股列表存储表'
```

# 创建股票日K线数据表（未经处理的原始数据）：包含股票代码、时间戳、开盘价、最高价、最低价、收盘价、成交量、成交额等字段
```SQL
CREATE TABLE IF NOT EXISTS qmt_stock_daily_ori (
    id BIGINT AUTO_INCREMENT PRIMARY KEY COMMENT 'ID',
    stock_code VARCHAR(20) NOT NULL COMMENT '股票代码',
    time TIMESTAMP NOT NULL COMMENT '时间戳',
    open DECIMAL(10,2) NOT NULL COMMENT '开盘价',
    high DECIMAL(10,2) NOT NULL COMMENT '最高价',
    low DECIMAL(10,2) NOT NULL COMMENT '最低价',
    close DECIMAL(10,2) NOT NULL COMMENT '收盘价',
    volume BIGINT NOT NULL COMMENT '成交量',
    amount DECIMAL(20,2) NOT NULL COMMENT '成交额',
    create_time TIMESTAMP DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
    update_time TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
    INDEX idx_stock_time (stock_code, time)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='股票日K线数据表';
```

# 创建股票周K线数据表（未经处理的原始数据）：包含股票代码、时间戳、开盘价、最高价、最低价、收盘价、成交量、成交额等字段
```SQL
CREATE TABLE IF NOT EXISTS qmt_stock_weekly_ori (
    id BIGINT AUTO_INCREMENT PRIMARY KEY COMMENT 'ID',
    stock_code VARCHAR(20) NOT NULL COMMENT '股票代码',
    time TIMESTAMP NOT NULL COMMENT '时间戳',
    open DECIMAL(10,2) NOT NULL COMMENT '开盘价',
    high DECIMAL(10,2) NOT NULL COMMENT '最高价',
    low DECIMAL(10,2) NOT NULL COMMENT '最低价',
    close DECIMAL(10,2) NOT NULL COMMENT '收盘价',
    volume BIGINT NOT NULL COMMENT '成交量',
    amount DECIMAL(20,2) NOT NULL COMMENT '成交额',
    create_time TIMESTAMP DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
    update_time TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
    INDEX idx_stock_time (stock_code, time)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='股票周K线数据表';
```

# 创建股票月K线数据表（未经处理的原始数据）：包含股票代码、时间戳、开盘价、最高价、最低价、收盘价、成交量、成交额等字段
```SQL
CREATE TABLE IF NOT EXISTS qmt_stock_monthly_ori (
    id BIGINT AUTO_INCREMENT PRIMARY KEY COMMENT 'ID',
    stock_code VARCHAR(20) NOT NULL COMMENT '股票代码',
    time TIMESTAMP NOT NULL COMMENT '时间戳',
    open DECIMAL(10,2) NOT NULL COMMENT '开盘价',
    high DECIMAL(10,2) NOT NULL COMMENT '最高价',
    low DECIMAL(10,2) NOT NULL COMMENT '最低价',
    close DECIMAL(10,2) NOT NULL COMMENT '收盘价',
    volume BIGINT NOT NULL COMMENT '成交量',
    amount DECIMAL(20,2) NOT NULL COMMENT '成交额',
    create_time TIMESTAMP DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
    update_time TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
    INDEX idx_stock_time (stock_code, time)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='股票月K线数据表';
```

# 创建交易日历表
```SQL
CREATE TABLE akshare_trade_calendar (
    id INT AUTO_INCREMENT PRIMARY KEY COMMENT '自增主键',
    trade_date DATE NOT NULL UNIQUE COMMENT '交易日期',
    year YEAR NOT NULL COMMENT '年份',
    month TINYINT NOT NULL COMMENT '月份',
    day TINYINT NOT NULL COMMENT '日期',
    weekday TINYINT NOT NULL COMMENT '星期几(1=周一,7=周日)',
    quarter TINYINT NOT NULL COMMENT '季度',
    week_of_year TINYINT NOT NULL COMMENT '年内第几周',
    is_month_end BOOLEAN DEFAULT FALSE COMMENT '是否月末交易日',
    is_quarter_end BOOLEAN DEFAULT FALSE COMMENT '是否季末交易日',
    is_year_end BOOLEAN DEFAULT FALSE COMMENT '是否年末交易日',
    create_time TIMESTAMP DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
    update_time TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
    
    INDEX idx_trade_date (trade_date),
    INDEX idx_year_month (year, month),
    INDEX idx_year_quarter (year, quarter),
    INDEX idx_weekday (weekday)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci COMMENT='AKShare交易日历表';
```